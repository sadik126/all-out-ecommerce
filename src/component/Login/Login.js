import React from 'react';
import Header from '../Header/Header';

const Login = () => {
    return (
        <div>
            <Header></Header>
            <h1>this is login</h1>
        </div>
    );
};

export default Login;